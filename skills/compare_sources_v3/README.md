# compare_sources

Provisional read-only comparison over the Grounded QA Application Port. Retrieval runs across the
current Space; the comparison answer must cite at least two distinct published sources or it is
refused. This package is invoked with a plain `question` (input_mode `question`) and no fixed
resource scope, so the router and `/compare` command both reach the same QA pipeline.
