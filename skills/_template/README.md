# Skill Template

Copy this directory to a non-underscore directory under a configured trusted Skill root, then
change the name and version. Keep workflows declarative and reference only package-local files.
