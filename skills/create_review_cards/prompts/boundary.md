# Review-card preview boundary

Generate only a citation-backed preview from the server-fixed DocumentVersion. No write port is
registered, so the workflow must report writes as blocked with zero side effects.
