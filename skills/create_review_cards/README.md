# create_review_cards

Provisional citation-backed preview. Derived knowledge persistence has no accepted Application Port,
so version 0.1.0 returns `SKILL_WRITE_REQUIRES_APPROVAL` until a durable approval is supplied,
with zero write side effects before approval.
