# create_review_cards

Provisional citation-backed preview. Derived knowledge persistence has no accepted Application Port,
so version 0.1.0 always returns `SKILL_WRITE_PORT_UNAVAILABLE` with zero write side effects.
