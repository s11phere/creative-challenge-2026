[project]
name = "model-gateway"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["httpx", "opentelemetry-api"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
include = ["model_gateway*"]
