[project]
name = "infrastructure"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "domain",
    "application",
    "pydantic-settings",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg",
    "redis",
    "dramatiq",
    "opentelemetry-api",
    "opentelemetry-sdk",
]
